// const { appendFile } = require('fs');
// const { getDate, getTime, getTimestamp } = require('./utils');

/**
 * 
 * @param {*} dir Lưu file tại vị trí
 * @param {*} data Lưu log vào folder
 */
module.exports = (dir, data) => {
    // const dataToLog = "[" + getTime(Date.now()) + "] " + data + "\n";
    // console.log("[" + getTimestamp(Date.now()) + "] " + data);
    // appendFile('./logs/' + dir + "/" + getDate(Date.now())+ '.txt', dataToLog, (err) => { if (err) throw err });
}